"""Versioning for the vpn CLI.

Keep this in sync with ``[project] version`` in pyproject.toml; the two must
never drift. `vpn --version` reports it via click's version_option.
"""

__version__ = "0.2.1"
