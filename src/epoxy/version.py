"""Versioning for the epoxy CLI.

Keep this in sync with ``[project] version`` in pyproject.toml; the two must
never drift. `epoxy --version` reports it via click's version_option.
"""

__version__ = "0.6.1"
